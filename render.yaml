services:
  - type: web
    name: pythostube
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false
